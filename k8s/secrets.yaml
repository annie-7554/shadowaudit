# Replace values before applying. Use sealed-secrets or external-secrets in production.
apiVersion: v1
kind: Secret
metadata:
  name: shadowaudit-secrets
  namespace: vulnwatch
type: Opaque
stringData:
  POSTGRES_USER: shadowaudit
  POSTGRES_PASSWORD: changeme-in-production
  WEBHOOK_URL: https://your-webhook-url
