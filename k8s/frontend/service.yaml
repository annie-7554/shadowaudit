apiVersion: v1
kind: Service
metadata:
  name: shadowaudit-frontend
  namespace: vulnwatch
spec:
  type: ClusterIP
  selector:
    app: shadowaudit-frontend
  ports:
    - port: 80
      targetPort: 80
